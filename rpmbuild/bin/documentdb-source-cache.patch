--- install_setup_pcre2.sh
+++ install_setup_pcre2.sh
@@ -51 +51,5 @@
-curl -L https://github.com/PCRE2Project/pcre2/releases/download/$PCRE_LIB_WITH_VERSION/$PCRE_LIB_WITH_VERSION.tar.gz -o ./$PCRE_LIB_WITH_VERSION.tar.gz
+if [ -s "$INSTALL_DEPENDENCIES_ROOT/$PCRE_LIB_WITH_VERSION.tar.gz" ]; then
+    cp "$INSTALL_DEPENDENCIES_ROOT/$PCRE_LIB_WITH_VERSION.tar.gz" ./$PCRE_LIB_WITH_VERSION.tar.gz
+else
+    curl -fL --retry 3 --retry-delay 2 --connect-timeout 15 --max-time 300 https://github.com/PCRE2Project/pcre2/releases/download/$PCRE_LIB_WITH_VERSION/$PCRE_LIB_WITH_VERSION.tar.gz -o ./$PCRE_LIB_WITH_VERSION.tar.gz
+fi
--- install_citus_indent.sh
+++ install_citus_indent.sh
@@ -31 +31,5 @@
-curl -L https://github.com/uncrustify/uncrustify/archive/${UNCRUSTIFY_REF}.tar.gz | tar xz
+if [ -s "$INSTALL_DEPENDENCIES_ROOT/${UNCRUSTIFY_REF}.tar.gz" ]; then
+    tar -xzf "$INSTALL_DEPENDENCIES_ROOT/${UNCRUSTIFY_REF}.tar.gz"
+else
+    curl -fL --retry 3 --retry-delay 2 --connect-timeout 15 --max-time 300 https://github.com/uncrustify/uncrustify/archive/${UNCRUSTIFY_REF}.tar.gz | tar xz
+fi
