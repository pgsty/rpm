--- a/src/biscuit.c
+++ b/src/biscuit.c
@@ -214,7 +214,11 @@
     amroutine->amstorage             = false;
     amroutine->amclusterable         = false;
     amroutine->ampredlocks           = false;
+#if PG_VERSION_NUM >= 180000
     amroutine->amcanparallel         = true;
+#else
+    amroutine->amcanparallel         = false;
+#endif
     amroutine->amcaninclude          = false;
     amroutine->amusemaintenanceworkmem = false;
     amroutine->amsummarizing         = false;
@@ -240,9 +244,15 @@
     amroutine->amendscan             = biscuit_endscan;
     amroutine->ammarkpos             = NULL;
     amroutine->amrestrpos            = NULL;
+#if PG_VERSION_NUM >= 180000
     amroutine->amestimateparallelscan = biscuit_estimateparallelscan;
     amroutine->aminitparallelscan    = biscuit_initparallelscan;
     amroutine->amparallelrescan      = biscuit_parallelrescan;
+#else
+    amroutine->amestimateparallelscan = NULL;
+    amroutine->aminitparallelscan    = NULL;
+    amroutine->amparallelrescan      = NULL;
+#endif
 
     PG_RETURN_POINTER(amroutine);
 }
--- a/src/biscuit_scan.c
+++ b/src/biscuit_scan.c
@@ -262,7 +262,7 @@
         {
             pdesc = (BiscuitParallelScanDesc *)
                         OffsetToPointer(scan->parallel_scan,
-                                        scan->parallel_scan->ps_offset_am);
+                                        BISCUIT_PARALLEL_SCAN_OFFSET(scan));
         }
 
         biscuit_collect_sorted_tids_parallel(
@@ -486,17 +486,12 @@
         return;
 
     /*
-     * Count this as one index search.  PostgreSQL 17+ tracks this counter
-     * in IndexScanDescData and displays it as "Index Searches" in EXPLAIN
-     * ANALYSE.  The AM is responsible for incrementing it; genam.h does not
-     * do so automatically.  Increment here, after the early-return guards,
-     * so only real searches are counted.
+     * Count this as one index search where PostgreSQL exposes index scan
+     * instrumentation. PG16/17 do not provide this counter in IndexScanDescData.
      */
 #if PG_VERSION_NUM >= 180000
     if (scan->instrument)
         scan->instrument->nsearches++;
-#elif PG_VERSION_NUM >= 170000
-    scan->xs_numIndexSearches++;
 #endif
 
     is_aggregate   = biscuit_is_aggregate_query(scan);
@@ -713,7 +708,7 @@
                 if (scan->parallel_scan != NULL)
                     pdesc = (BiscuitParallelScanDesc *)
                                 OffsetToPointer(scan->parallel_scan,
-                                                scan->parallel_scan->ps_offset_am);
+                                                BISCUIT_PARALLEL_SCAN_OFFSET(scan));
               
                 biscuit_collect_sorted_tids_parallel(
                     so->index, result, pdesc,
@@ -907,7 +902,7 @@
                     if (scan->parallel_scan != NULL)
                         pdesc = (BiscuitParallelScanDesc *)
                                     OffsetToPointer(scan->parallel_scan,
-                                                    scan->parallel_scan->ps_offset_am);
+                                                    BISCUIT_PARALLEL_SCAN_OFFSET(scan));
                   
                      biscuit_collect_sorted_tids_parallel(
                         so->index, candidates, pdesc,
--- a/src/biscuit_preload.c
+++ b/src/biscuit_preload.c
@@ -53,6 +53,9 @@
 #include "tcop/tcopprot.h"
 #include "utils/memutils.h"
 #include "utils/snapmgr.h"
+#if PG_VERSION_NUM < 170000
+#include "utils/wait_event.h"
+#endif
 #include "access/xact.h"
 
 /* PG 14 moved SignalHandlerForConfigReload here */
--- a/src/biscuit_tid.c
+++ b/src/biscuit_tid.c
@@ -549,7 +549,7 @@
 
     pdesc = (BiscuitParallelScanDesc *)
                 OffsetToPointer(scan->parallel_scan,
-                                scan->parallel_scan->ps_offset_am);
+                                BISCUIT_PARALLEL_SCAN_OFFSET(scan));
 
     return (pdesc->num_participants > 1) ? pdesc->num_participants - 1 : 2;
 }
@@ -635,7 +635,7 @@
 
     pdesc = (BiscuitParallelScanDesc *)
                 OffsetToPointer(scan->parallel_scan,
-                                scan->parallel_scan->ps_offset_am);
+                                BISCUIT_PARALLEL_SCAN_OFFSET(scan));
 
     /* Clear all slots and reset the initialized flag to 0. */
     pdesc->total_tids = 0;
--- a/src/biscuit_tid.h
+++ b/src/biscuit_tid.h
@@ -82,6 +82,12 @@
 /* Maximum participants (1 leader + up to this many workers). */
 #define BISCUIT_MAX_PARALLEL_WORKERS  64
 
+#if PG_VERSION_NUM >= 180000
+#define BISCUIT_PARALLEL_SCAN_OFFSET(scan) ((scan)->parallel_scan->ps_offset_am)
+#else
+#define BISCUIT_PARALLEL_SCAN_OFFSET(scan) ((scan)->parallel_scan->ps_offset)
+#endif
+
 /*
  * BiscuitWorkerSlot — the TID index range assigned to one participant.
  * start is inclusive, end is exclusive.  Both are indices into the local
