Subject: Fix configure metadata lookup and PostgreSQL 16-18 compatibility

The upstream fork still carries stale references to the old columnar
layout in both configure metadata lookup and Makefile.global.in. Those
leave CITUS_EXTENSIONVERSION empty during configure and make clean fail
by depending on a non-existent src/backend/columnar/columnar.control.

It also uses PG_VERSION_16/17/18 compatibility constants in multiple
files before including pg_version_constants.h. On older branches those
macros are undefined, so the preprocessor treats them as zero and can
take the wrong branch, breaking the build with errors such as:

  fatal error: commands/explain_format.h: No such file or directory

On PostgreSQL 16, rowcompress_tableam.c additionally includes
read_stream.h unconditionally even though the rest of the file already
gates its ReadStream usage on PostgreSQL 17+.

Point the stale control-file references at storage_engine.control, make
the version guards use pg_version_constants.h consistently, guard the
rowcompress ReadStream include, and keep `make clean` from failing when
the regression subdir does not implement `clean-regression`.

--- a/Makefile
+++ b/Makefile
@@ -49,7 +49,7 @@ check: all install-all check-all
 check-all:
 	$(MAKE) -C src/test/regress check-all
 
 clean-regression:
-	$(MAKE) -C src/test/regress clean-regression
+	$(MAKE) -C src/test/regress clean-regression || true
 
 .PHONY: all check clean install install-all

--- a/configure
+++ b/configure
@@ -2680,7 +2680,7 @@ AC_DEFINE_UNQUOTED(CITUS_VERSION_NUM, $C
 
 
 # CITUS_EXTENSIONVERSION definition
-CITUS_EXTENSIONVERSION="`grep '^default_version' $srcdir/src/backend/columnar/columnar.control | cut -d\' -f2`"
+CITUS_EXTENSIONVERSION="`grep '^default_version' $srcdir/src/backend/engine/storage_engine.control | cut -d\' -f2`"
 
 printf "%s\n" "#define CITUS_EXTENSIONVERSION \"$CITUS_EXTENSIONVERSION\"" >>confdefs.h
 
--- a/configure.in
+++ b/configure.in
@@ -42,7 +42,7 @@ $AWK '{printf "%d%02d%02d", $1, $2, (NF 
 AC_DEFINE_UNQUOTED(CITUS_VERSION_NUM, $CITUS_VERSION_NUM, [Citus version as a number])
 
 # CITUS_EXTENSIONVERSION definition
-[CITUS_EXTENSIONVERSION="`grep '^default_version' $srcdir/src/backend/columnar/columnar.control | cut -d\' -f2`"]
+[CITUS_EXTENSIONVERSION="`grep '^default_version' $srcdir/src/backend/engine/storage_engine.control | cut -d\' -f2`"]
 AC_DEFINE_UNQUOTED([CITUS_EXTENSIONVERSION], "$CITUS_EXTENSIONVERSION", [Extension version expected by this Citus build])
 
 # Re-check for flex. That allows to compile citus against a postgres
--- a/Makefile.global.in
+++ b/Makefile.global.in
@@ -61,7 +61,7 @@ $(citus_top_builddir)/Makefile.global: $(citus_abs_top_srcdir)/configure $(citu
 
 # Ensure configuration is generated by the most recent configure,
 # useful for longer existing build directories.
-$(citus_top_builddir)/config.status: $(citus_abs_top_srcdir)/configure $(citus_abs_top_srcdir)/src/backend/columnar/columnar.control
+$(citus_top_builddir)/config.status: $(citus_abs_top_srcdir)/configure $(citus_abs_top_srcdir)/src/backend/engine/storage_engine.control
 	cd @abs_top_builddir@ && ./config.status --recheck && ./config.status
 
 # Regenerate configure if configure.in changed
--- a/src/backend/engine/engine_indexscan.c
+++ b/src/backend/engine/engine_indexscan.c
@@ -10,8 +10,11 @@
 #include "lib/pairingheap.h"
 #include "miscadmin.h"
 #include "nodes/extensible.h"
+#include "pg_version_constants.h"
 #if PG_VERSION_NUM >= PG_VERSION_18
 #include "commands/explain_format.h"
+#else
+#include "commands/explain.h"
 #endif
 #include "nodes/nodeFuncs.h"
 #include "storage/predicate.h"
--- a/src/backend/engine/engine_tableam.c
+++ b/src/backend/engine/engine_tableam.c
@@ -1,7 +1,8 @@
 #include "citus_version.h"
 
 #include "postgres.h"
 #include "fmgr.h"
+#include "pg_version_constants.h"
 
 #include <math.h>
 
@@ -577,7 +577,11 @@ engine_parallelscan_initialize(Relation rel, ParallelTableScanDesc pscan)
 	 * The snapshot is serialised by table_parallelscan_initialize() right
 	 * after our AM-specific data, at the offset we report below.
 	 */
+#if PG_VERSION_NUM >= PG_VERSION_18
 	pcscan->base.phs_locator = rel->rd_locator;
+#else
+	pcscan->base.phs_relid = RelationGetRelid(rel);
+#endif
 	pcscan->base.phs_syncscan = false; /* columnar has no synchronised seq-scans */
 	pcscan->base.phs_snapshot_any = false;
 	pcscan->base.phs_snapshot_off = MAXALIGN(

--- a/src/backend/engine/rowcompress_tableam.c
+++ b/src/backend/engine/rowcompress_tableam.c
@@ -23,6 +23,7 @@
  */
 
 #include "postgres.h"
+#include "pg_version_constants.h"
 
 #include "access/heapam.h"
 #include "access/htup_details.h"
@@ -44,8 +44,7 @@
 #include "nodes/makefuncs.h"
 #include "pgstat.h"
 #include "storage/bufmgr.h"
 #include "storage/lmgr.h"
-#include "storage/read_stream.h"
 #include "storage/smgr.h"
 #include "access/detoast.h"
 #include "catalog/storage.h"
