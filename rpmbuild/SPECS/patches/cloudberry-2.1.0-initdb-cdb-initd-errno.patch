--- a/src/bin/initdb/initdb.c
+++ b/src/bin/initdb/initdb.c
@@ -2022,6 +2022,7 @@ setup_cdb_schema(FILE *cmdfd)
 
 	/* Collect all files with .sql suffix in array. */
 	nscripts = 0;
+	errno = 0;
 	while ((file = readdir(dir)) != NULL)
 	{
 		int			namelen = strlen(file->d_name);
